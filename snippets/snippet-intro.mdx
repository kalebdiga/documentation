BlitzBoard public API documentation uses Mintlify MDX.

Prefer linking to `/authentication`, `/quickstart`, and `/api-reference/introduction` instead of duplicating auth or base-path details on every page.
