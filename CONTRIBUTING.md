# Contributing

1. Edit MDX pages under this repository.
2. Preview with `mint dev`.
3. Keep auth and base-path details in `authentication.mdx` / `concepts.mdx`; link from endpoint pages instead of duplicating.
4. Endpoint paths must match the Nest public controllers under `v1/blitz-api`.
